{
  "_comment": "Optional: use this instead of repeated CLI flags when auditing several projects/clusters on a schedule. The script itself only takes CLI args today — this file documents the shape you'd loop over from a wrapper shell script or cron job.",
  "auth": {
    "client_id_env": "ATLAS_CLIENT_ID",
    "client_secret_env": "ATLAS_CLIENT_SECRET"
  },
  "days": 7,
  "targets": [
    { "group_id": "5f8a1b2c3d4e5f6a7b8c9d0e", "cluster": "prod-cluster-0" },
    { "group_id": "5f8a1b2c3d4e5f6a7b8c9d0e", "cluster": "prod-cluster-1" },
    { "group_id": "6a1b2c3d4e5f6a7b8c9d0e1f" }
  ]
}
